<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissões -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_eudaimoniaforum"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_eudaimoniaforum"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_Metas_2"
        tools:targetApi="31">

        <!-- Activities Novas -->
        <activity android:name=".ChatActivity" />
        <activity android:name=".ConversasActivity" />
        <activity android:name=".RecuperarSenhaActivity" />

        <activity
            android:name=".NotificacaoActivity"
            android:exported="false" />
        <activity
            android:name=".ComentarioActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VisualizarPerfilActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".EditarAbstinenciaActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".EditarPerfilActivity" />
        <activity android:name=".NovoPostActivity" />
        <activity android:name=".ModeracaoActivity" />
    </application>

</manifest>